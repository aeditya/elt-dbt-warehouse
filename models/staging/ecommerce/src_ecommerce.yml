version: 2

sources:
  - name: raw
    database: ECOMMERCE_DW
    schema: RAW

    tables:
      - name: online_retail
      - name: customers_raw
        columns:
          - name: customer_id
            tests: 
            - not_null
            - unique
          
          

